
{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4 text-primary">Edit Produit</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('products/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-6">
                <a href="{{ path('app_products_index') }}" class="btn btn-secondary">Back to List</a>
            </div>
            <div class="col-md-6 text-right">
                {{ include('products/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
