<form method="post" action="{{ path('app_beauty_delete', {'id': beauty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beauty.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

